# AppTuner Environment Variables
# Copy this file to .env and fill in your actual values

# ====================
# SUPABASE CONFIGURATION
# ====================
# Get these from your Supabase project settings:
# https://app.supabase.com/project/YOUR_PROJECT/settings/api

# Your Supabase project URL (e.g., https://xxxxxxxxxxxxx.supabase.co)
VITE_SUPABASE_URL=

# Your Supabase anonymous key (public, safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=

# ====================
# STRIPE CONFIGURATION
# ====================
# Get these from your Stripe dashboard:
# https://dashboard.stripe.com/apikeys

# Stripe publishable key (public, safe to expose in frontend)
# Use test key for development: pk_test_xxxxx
# Use live key for production: pk_live_xxxxx
VITE_STRIPE_PUBLISHABLE_KEY=

# ====================
# STRIPE PRICE IDS
# ====================
# Create these in Stripe dashboard under Products:
# https://dashboard.stripe.com/products
# Each price has a unique ID like price_xxxxxxxxxxxxx

# Monthly subscription price ID
VITE_STRIPE_PRICE_MONTHLY=

# Yearly subscription price ID
VITE_STRIPE_PRICE_YEARLY=

# Lifetime purchase price ID
VITE_STRIPE_PRICE_LIFETIME=

# ====================
# API SERVER (VPS relay — handles Stripe checkout + webhooks)
# ====================
# URL of the VPS relay server (used by frontend to call payment APIs)
VITE_API_BASE_URL=https://relay.apptuner.io

# Stripe secret key — keep this private, set in Coolify env vars, never use VITE_ prefix
STRIPE_SECRET_KEY=

# Webhook signing secret — get from Stripe dashboard > Webhooks
# Add endpoint: https://relay.apptuner.io/api/stripe-webhook
# Then copy the signing secret here and set in Coolify env vars
STRIPE_WEBHOOK_SECRET=

# Supabase service role key — has full database access, keep private
# Get from Supabase dashboard > Settings > API > service_role key
# Set in Coolify env vars for the relay server
SUPABASE_SERVICE_ROLE_KEY=

# ====================
# WEB3FORMS CONFIGURATION
# ====================
# For contact and support form submissions (250 free submissions/month)
# Get your access key from: https://web3forms.com/
# Sign up (free), create ONE form, and copy the access key (UUID format)
# Example: a1b2c3d4-e5f6-7890-abcd-ef1234567890
# Both Contact and Support use the same form - they're distinguished by message prefix

# Web3Forms access key (all submissions go to info@apptuner.io)
VITE_WEB3FORMS_ACCESS_KEY=

# ====================
# DEVELOPMENT NOTES
# ====================
# 1. NEVER commit the .env file to git (it's in .gitignore)
# 2. Use Stripe test mode during development
# 3. Supabase has different keys for different environments
# 4. All VITE_ prefixed variables are exposed to the frontend
# 5. Keep your secret keys secure and never share them
